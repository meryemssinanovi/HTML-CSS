// 4.

const brojevi5 = [-5, 4, 3, 10, 14, -5];
const naredni_niz = brojevi5.map((element) =>
  element < 0 ? element * -1 : element ** 2
);
console.log(naredni_niz);

// 1.
const prvi_niz = ["krofna", "kifla", "knjiga", "posao"];
const novi_niz = prvi_niz.map((element) => e.toUpperCase());
console.log(novi_niz);

// 2.

const br3 = brojevi.map((element) => element ** 3);
console.log(br3);
